org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.parallel=false

# Mod metadata
mod_id=fantasticregions
mod_name=FantasticRegions
mod_display_name=Fantastic Regions
mod_version=1.0.0-1.20.1
mod_group_id=com.athensmc.fantasticregions
mod_authors=AthensMC team

# Licence: this mod links against Yet Another World Protector (AGPL v3), so it is AGPL v3 too.
mod_license=AGPL v3

# Platform
minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.1.3
forge_version_range=[47,)
loader_version_range=[47,)

# The host mod this one drives. Its jar goes in libs/ and is NOT bundled into ours.
yawp_version_range=[0.6,)


# The host mod's jar, compiled against but never bundled. It is the copy already in the repository
# root rather than a duplicate under libs/, so there is only one file to replace when YAWP updates.
yawp_jar=../yawp-1.20.1mohistfix.jar
